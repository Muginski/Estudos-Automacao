# Config da API para RestAssured — lido em AlunosTest:25 e AuthTest:23
# baseUrl: host do SUT (pode sobrescrever via mvn test -DbaseUrl=...)
baseUrl=http://localhost:3000
# basePath: prefixo da API (ex: /api -> POST /api/alunos)
basePath=/api
